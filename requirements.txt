# 核心模块（ai_brain_entity.py / swarm.py / tests/）零依赖，仅需 Python 3.8+ 标准库。
# 以下依赖仅供实验复现脚本（experiments.py）绘图与汇总使用：
matplotlib>=3.5
seaborn>=0.12
pandas>=1.3

# ============ 可选依赖（不装也能完整运行）============
# 真实多模态编码（perceive_image / perceive_audio）：
# 未安装时自动降级为确定性哈希伪 embedding，核心功能与实验全部不受影响。
# transformers>=4.30   # CLIP 图像 embedding
# torch>=2.0           # CLIP/Whisper 推理后端
# pillow>=9.0          # 图像解码
# openai-whisper       # 音频 embedding

# N-Omniglot 原始数据转换（仅 export_nomniglot.py 一次性导出需要；
# 导出的 data/nomniglot_*.json 由 nomniglot.py 零依赖加载，不需要 dv）
# dv>=1.0              # aedat4 事件文件解析（Python 3.12 需 imp shim，已在脚本内置）
